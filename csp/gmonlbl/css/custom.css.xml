<?xml version="1.0" encoding="UTF-8"?>
<Export generator="Cache" version="25">
<CSP name="css/custom.css" application="/csp/user/gmonlbl/"><![CDATA[
/* Navbar's styles */
.navbar-btn {
    margin-left: 7px;
    margin-right: 7px;
    padding-left: 5px;
    padding-right: 5px;
    width: 100px;
}

.navbar {
    margin-bottom: 1px;    
} 

.pull-right {
    height: 50px;   
}

.status {
    width: 250px;
    height: 50px;
    display: block; 
    font-size: 20px;
    text-align: right;
    font-weight: 200;
    color: #777777;
    text-shadow: 0 1px 0 #ffffff;
    line-height: 50px;
    font-family: "Helvetica Neue",Helvetica,Arial,sans-serif;
    cursor: default;
}
/*-----------------------*/


/* Popup styles */
.popup-start {
  width: 575px;
  float: left;
  height: 30px;
  position: absolute;
  left: 172px;
  display: none;
}

.input-search {
    background: linear-gradient(to bottom,#6485F5 0%,#275AF2 100%);
    height: 40px;
    border-bottom: 1px solid #1E4CE8;
    -webkit-border-radius: 3px;
    -moz-border-radius: 3px;
    border-radius: 3px;
}
    
.label-search {
      float: right;
      display: inline-block;
      color: #fff;
      font-size: 16px;
      font-weight: bold;
      text-shadow: 0 0px 2px rgba(0,0,0,0.5);
      line-height: 40px;
      text-align: center;
      cursor: default;
      -moz-user-select: none;
      -khtml-user-select: none;
      -webkit-user-select: none;
      -o-user-select: none;
      user-select: none;
    }

.btn-search {
    float:right;
    background: #55ACEE;
    color: #fff;
    text-shadow: none;
    height:32px;
    margin:4px 12px 0 0;
    @include border-radius(3px);
    box-shadow: none; 
    border: none;
    vertical-align: middle; 
    &:focus {outline-width:0;}
}

#search {
    float: right;
    margin: 4px 12px 0 15px;
    padding: 4px 10px;
    border: 1px solid #3E559F;
    width: 291px;
    height: 32px;
    border-radius: 3px;
    line-height: 12px;
    font-size: 12px;
    &:focus {outline-width:0;}
}

/* -------------------------------- */

.jumbotron {
    height: 330px;  
    margin: 30px;
    padding-top: 150px;
    background: #fff;
}


/* Alerts  styles */

.alert {
    clear:both;   
}

.alert-routine {
    margin-top: 20px;
    width: 90%;
    margin-left: auto;
    margin-right: auto;
}


.alert p {
    display: inline-block;
    margin-bottom: 0;
}

/* ----------------------- */


/* Table styles */

.tbl-container {
    width: 80%;
    margin: auto;
    margin-top: 80px;
}

#tblSummary {
    cursor: pointer;
}

#tblSummary th:hover {
    background-color: #d1ceed;
}

#tblSummary th:first-child:hover {
    background-color: #fff;
    cursor: default;
}

#tblSummary td.rownums:before { 
  content: counter(rownum) " "; 
}

th {
  text-align: center;
  -moz-user-select: none;
  -khtml-user-select: none;
  -webkit-user-select: none;
  -o-user-select: none;
  user-select: none;
}

tbody tr {
  counter-increment : rownum ;
}
tbody { 
  counter-reset: rownum; 
}

#tblRoutineDetails {
     table-layout: fixed;   
}

#tblRoutineDetails td.rownums:before { 
  content: counter(rownum) " "; 
}

#tblRoutineDetails th:nth-child(1) {
     width: 50px;   
}

#tblRoutineDetails th:nth-child(2), #tblRoutineDetails th:nth-child(3), #tblRoutineDetails th:nth-child(4) {
     width: 100px;   
}

#tblRoutineDetails th:nth-child(5) {
     width: 620px;   
}

td pre {
    margin: 0;   
    padding: 0;
    background-color: transparent;
    border: none;
}

/* ---------------------------- */

/* General styles */
.active {
    display:block;
}

.hidden {
     display: none;   
}

.center-align {
    text-align: center;    
}

.right-align {
    text-align: right; 
}

.center-valign {
     vertical-align: middle;   
}
/* ---------------- */]]></CSP>
</Export>
